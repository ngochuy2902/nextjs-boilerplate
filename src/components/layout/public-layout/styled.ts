import styled from 'styled-components';
import { Layout } from 'antd';

export const PublicLayoutStyled = styled(Layout)`
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
`;
